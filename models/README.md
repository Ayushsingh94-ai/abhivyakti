# This folder stores trained model files (.h5, .pkl, .onnx)
# Models are NOT committed to GitHub (too large).
# Upload to HuggingFace Hub instead:
#   huggingface-cli upload YOUR_USERNAME/abhivyakti models/isl_model.h5
